name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        registry-url: 'https://registry.npmjs.org'

    - name: Setup pnpm
      uses: pnpm/action-setup@v2
      with:
        version: 8.15.1

    - name: Install dependencies
      run: |
        pnpm install --frozen-lockfile
        cd cli && pnpm install --frozen-lockfile && cd ..
        cd vscode-extension && pnpm install --frozen-lockfile && cd ..

    - name: Build all components
      run: |
        cd cli && pnpm run build && cd ..
        cd vscode-extension && pnpm run compile && cd ..

    - name: Package CLI
      run: |
        cd cli
        pnpm pack
        mv *.tgz ../ade-core.tgz

    - name: Package VSCode Extension
      run: |
        cd vscode-extension
        pnpm run package
        mv *.vsix ../ade-extension.vsix

    - name: Create Release
      uses: softprops/action-gh-release@v1
      with:
        files: |
          ade-core.tgz
          ade-extension.vsix
        body: |
          ## ADE Platform Release

          ### Installation

          #### CLI Tool
          ```bash
          pnpm install -g ade-core.tgz
          ```

          #### VSCode Extension
          1. Download `ade-extension.vsix`
          2. In VSCode: Extensions → ... → Install from VSIX

          ### What's Changed
          See [CHANGELOG.md](https://github.com/phdsystems/ade-platform/blob/main/CHANGELOG.md)

    - name: Publish CLI to npm (optional)
      if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')
      run: |
        cd cli
        pnpm publish --access public
      env:
        NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
      continue-on-error: true