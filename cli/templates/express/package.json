{
  "name": "{{serviceName}}",
  "version": "1.0.0",
  "type": "module",
  "description": "{{Domain}} domain - {{ServiceName}} service",
  "main": "src/index.mjs",
  "scripts": {
    "start": "node --enable-source-maps src/index.mjs",
    "start:cluster": "node --enable-source-maps src/cluster.mjs",
    "dev": "node --watch --enable-source-maps src/index.mjs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.1",
    "helmet": "^7.1.0",
    "compression": "^1.7.4",
    "express-rate-limit": "^7.1.5",
    "pino": "^8.17.2",
    "pino-http": "^9.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/express": "^4.17.21",
    "@types/compression": "^1.7.5"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}